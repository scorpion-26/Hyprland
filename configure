#!/bin/sh

# Delete old run

rm -rf build

# clang
# export CC=clang
# export CXX=clang++
# export CC_LD=lld
# export CXX_LD=lld
# export CFLAGS="-march=native -Ofast -pipe -fomit-frame-pointer"
# export CXXFLAGS="-march=native -Ofast -pipe -fomit-frame-pointer"
# meson setup build -Db_lto=true -Db_lto_mode=thin -Dbuildtype=release

# gcc
export CC=gcc
export CXX=g++
export CFLAGS="-march=native -Ofast -pipe -fomit-frame-pointer"
export CXXFLAGS="-march=native -Ofast -pipe -fomit-frame-pointer"
meson setup build -Db_lto=true -Dbuildtype=release
